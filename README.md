# EmpireNode 2016 Site

## Running with Docker


```
# Development Mode:
docker-compose build
docker-compose up

# Production Mode:
docker-compose -f docker-compose.prod.yml build
docker-compose -f docker-compose.prod.yml up
```
